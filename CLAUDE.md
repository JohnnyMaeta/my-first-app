# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際に Claude Code (claude.ai/code) に向けたガイダンスを提供します。

## プロジェクト概要

ブラウザで動く横スクロールアクションゲーム（サイドスクローラー）。ビルドステップ・フレームワーク・パッケージマネージャーなしの静的サイトとして構築されており、アプリ全体が以下の3ファイルで完結しています。

- `index.html` — ページ構造。描画用の `<canvas id="game-canvas">` と、スコア表示・スタート/リトライボタン付きのready/ゲームオーバー画面を担うHTMLオーバーレイ（`#ui-overlay`）で構成。
- `style.css` — canvasコンテナとオーバーレイ画面のレイアウト・UIスタイル。
- `game.js` — ゲームロジック全体。単一のIIFEにまとめられており、モジュール/importは使用していません。

## ゲームの実行方法

ビルド・lint・テストのツールは設定されていません。`index.html` をブラウザで直接開くか、任意の静的ファイルサーバー（例: `python3 -m http.server`）でこのディレクトリを配信してアクセスしてください。相対モジュールのimportがないため、どちらの方法でも動作します。

## アーキテクチャ（game.js）

ゲームは単一の `requestAnimationFrame` ループ（`loop`）で動作し、シンプルなステートマシンによって制御されています。

- `STATE.READY` → `STATE.PLAYING` → `STATE.GAMEOVER`（リトライ時は再び `PLAYING` へ）
- 状態遷移は `startGame()` と `gameOver()` を通じて行われ、これらは `#message-screen` オーバーレイの表示/非表示も切り替えます（プレイ中は非表示、それ以外はタイトル/スコアのテキストとともに表示）。

`update(dt)` 内でのコアループの処理内容（実行順）:
1. 時間経過とともに `scrollSpeed` と `score` を増加させる（長く生き残るほど難易度が上がる）。
2. `player` に重力/ジャンプの物理演算を適用する（`vy` を用いたシンプルなAABB、`GROUND_Y` で地面にクランプ）。
3. ランダムなタイマー（`spawnTimer`）で障害物を生成する。スコアが上がるほど生成間隔が短くなる。
4. 障害物を `scrollSpeed * dt` 分だけ左へ移動させ、画面外に出たものを除去する。
5. プレイヤーと各障害物のAABB衝突判定（`rectsOverlap`）を行い、衝突したら `gameOver()` を呼ぶ。

描画（`draw()`）は毎フレーム「クリアして描き直す」シンプルな方式です。地面・障害物・プレイヤーの順にcanvas 2Dの塗りつぶし矩形として描画しており、スプライトや画像は使用していません。

入力は `handleActionPress()` に一本化されています。`READY`/`GAMEOVER` 時はゲームの開始/再開、`PLAYING` 時はジャンプをトリガーします。これはスタートボタンのクリック、`Space`/`ArrowUp` キーの押下、canvasの `pointerdown`（タップ対応）に紐付けられており、すべての入力経路が同じコードパスを共有します。

## コーディング上の慣習

- 素のES6を使用。TypeScriptや外部依存はなし。
- ゲーム定数（canvasサイズ、重力、ジャンプ速度、地面の位置など）は `game.js` の先頭でトップレベルの `const` として定義されています。難易度や操作感を調整する際は、コード中にマジックナンバーを直接書くのではなく、これらの定数を編集してください。
